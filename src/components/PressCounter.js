import React, { Component } from 'react'

export class PressCounter extends Component {
  render() {
    return (
      <div>
        <button>Pressed X times</button>
      </div>
    )
  }
}

export default PressCounter
